# Binance Futures Testnet Trading Bot — dependencies

# HTTP client (requests is already a python-binance dep, listed explicitly for clarity)
requests>=2.31.0

# Environment variable loading
python-dotenv>=1.0.0

# CLI framework
typer>=0.12.0

# Pretty terminal output
rich>=13.7.0
